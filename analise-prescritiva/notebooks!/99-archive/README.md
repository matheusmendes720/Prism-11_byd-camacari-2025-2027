# 99-archive — Arquivos antigos (não essenciais)

Esta pasta contém arquivos que não são essenciais para o case study mas foram preservados por questões de auditoria.

## Conteúdo

- `.ipynb_checkpoints/` — checkpoints do Jupyter Lab (auto-gerados ao salvar notebooks)
- `__pycache__/` — bytecode Python cacheado (regenerado em cada execução)
- `analise-prescritiva` — arquivo de teste antigo (48 KB)
- `b-01-ptax-garch.ipynb` — versão antiga vazia (0 bytes)

## Status

Tudo aqui é **descartável**. Pode ser apagado sem perder nada do case study.

## Quando limpar

```bash
cd ../
rm -rf 99-archive/
```

(Use isso quando quiser reduzir tamanho do repo; arquivos podem ser regenerados se necessário.)